// FILE NÀY ĐÃ ĐƯỢC THAY THẾ BỞI src/auth.ts (NextAuth v5 thật)
// Giữ lại để không break các import cũ nhưng KHÔNG dùng trực tiếp nữa.
// Toàn bộ cấu hình thật nằm tại: src/auth.ts + src/auth.config.ts

export { authConfig } from "@/auth.config";
